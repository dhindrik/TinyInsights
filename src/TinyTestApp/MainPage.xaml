<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TinyTestApp.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">

            <Button
                x:Name="PageViewButton"
                Clicked="PageViewButton_OnClicked"
                Text="Track page view" />

            <Button
                x:Name="EventButton"
                Clicked="EventButton_OnClicked"
                Text="Track event" />
            <Button
                x:Name="ExceptionButton"
                Clicked="ExceptionButton_OnClicked"
                Text="Track exception" />
            <Button
                x:Name="TrackHttpButton"
                Clicked="TrackHttpButton_OnClicked"
                Text="Track http request" />
            <Button
                x:Name="TrackFailedHttpButton"
                Clicked="TrackFailedHttpButton_Clicked"
                Text="Track failed http request" />
            <Button
                x:Name="CrashButtom"
                Clicked="CrashButtom_OnClicked"
                Text="Crash app" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
